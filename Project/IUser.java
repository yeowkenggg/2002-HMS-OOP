/**
 * An interface class which manage the user related operations
 */
public interface IUser {
    /**
     * An abstract method for subclasses to implement their own menus
     */
    void displayMenu();
}
